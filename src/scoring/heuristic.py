import pandas as pd


def score_players(df: pd.DataFrame, weights: dict) -> pd.Series:
    # Weighted sum of normalized features → draft score
    # weights keys: adp, projection, wopr, hvt, xfp, adp_trend
    # TODO: implement
    raise NotImplementedError
