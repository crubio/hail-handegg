import pandas as pd


def extract_floor_ceiling(projections: pd.DataFrame) -> pd.DataFrame:
    # TODO: implement — returns df with floor_pts and ceiling_pts columns
    raise NotImplementedError


def apply_injury_risk(df: pd.DataFrame) -> pd.Series:
    # Multiplier (0–1) applied to baseline projection based on injury history
    # TODO: implement
    raise NotImplementedError
